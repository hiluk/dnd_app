import 'package:flutter/material.dart';

class DndColors {
  static const primary = Color.fromRGBO(166, 38, 57, 0.7);
  static const onPrimary = Colors.white;
  static const secondary = Color.fromRGBO(86, 73, 78, 0.7);
  static const onSecondary = Colors.white;
  static const backround = Color.fromRGBO(162, 156, 155, 0.9);
  static const onThirdy = Colors.white;
  static const fourth = Color.fromRGBO(162, 156, 155, 0.7);
  static const onFourth = Colors.black;
}
